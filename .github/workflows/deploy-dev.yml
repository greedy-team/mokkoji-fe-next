name: Develop Deploy

on:
  push:
    branches: [develop]
  workflow_dispatch:
    inputs:
      cmd:
        description: 'restart | stop | deploy'
        required: false
        default: 'deploy'

jobs:
  call-deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      env: dev
      cmd: ${{ github.event.inputs.cmd || 'deploy' }}
    secrets: inherit
