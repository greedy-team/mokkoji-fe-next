// import loadDevTodo from '@/shared/lib/loadDevTodo';
// import { headers } from 'next/headers';
// import DevTodo from './dev-to-do';

// async function DevTodoLoader() {
//   const pathname = (await headers()).get('x-invoke-path') || '/';

//   const todoData = loadDevTodo(pathname);

//   if (!todoData) return null;

//   return <DevTodo {...todoData} />;
// }

// export default DevTodoLoader;
