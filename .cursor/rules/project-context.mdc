---
description: 프로젝트 전반 컨텍스트 및 코드 규칙
globs:
alwaysApply: true
---

## 모꼬지 — 세종대 동아리 플랫폼

- Next.js 15.3 (App Router, Turbopack, standalone), React 19, TS 5.8
- Tailwind CSS v4 + shadcn/ui (new-york) + CVA + tailwind-merge
- API: ky (`serverApi` 비인증, `api` 인증+미들웨어 토큰갱신)
- 인증: `app-session` httpOnly 쿠키, JWT access/refresh token
- FSD: app → views → widgets → features → entities → shared (상위→하위만 import)
- 코드: function-declaration, `@/*` alias, Airbnb ESLint + Prettier, cn()
- 폼: useReducer, URL: nuqs, 세션: AppSessionProvider (Context)
- 커밋: `[#이슈번호|no-issue] type: subject`, Husky + lint-staged
- 패키지: pnpm
